import { RedisType } from "./typesAndDefaults.js";
export declare class RedisStore {
    private client;
    timeFrame: number;
    constructor(client: RedisType, timeFrame: number);
    increment(key: string): Promise<number>;
}
